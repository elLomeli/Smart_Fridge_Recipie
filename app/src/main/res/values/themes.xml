<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Recipies" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/txt</item>
        <item name="colorPrimaryVariant">@color/txt</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/txt</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="21">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="SplashScreen" parent="Theme.Recipies">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="SwitchStyle" parent="Widget.AppCompat.CompoundButton.Switch">>
        <item name="android:thumbTextPadding">10dp</item>
        <item name="android:thumbTextPadding">10dp</item>
        <item name="android:thumbTint">@color/fondoSplash</item>
        <item name="android:trackTint">@color/IconGray</item>
    </style>



</resources>